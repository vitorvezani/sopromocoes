<div class="row page-header">
  <div class="col-md-6">
    <h3><%= @category.name.titleize %></h3>
  </div>
  <div class="col-md-6">
    <div class="pull-right">
      <%= render 'shared/security/edit', path: edit_category_path(@category) if is_admin? %>
      <%= render 'shared/security/delete', item: @category if is_admin? %>
    </div>
  </div>
</div>

<dl class="dl-horizontal">
</dl>
