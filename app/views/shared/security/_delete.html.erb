<% if owns_record(item) or is_admin? %>
  <%= link_to item, method: :delete, data: { confirm: 'Você tem certeza?' }, class: 'btn btn-danger' do %>
    <span class="glyphicon glyphicon-remove"></span>
    Excluir
  <% end %>
<% end %>
