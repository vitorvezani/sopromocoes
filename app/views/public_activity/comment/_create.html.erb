<li class="list-group-item">
  <span class="glyphicon glyphicon-comment"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.name : 'Guest' %></strong>
  <% if a.trackable %>
    adicionou um comentário em <%= link_to a.trackable.commentable.name.truncate(35), commentable_link(a.trackable.commentable) %>.
  <% else %>
    adicionou um comentário que foi deletado.
  <% end %>
</li>
